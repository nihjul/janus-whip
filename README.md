# Janus-WHIP

# Requirements
- Just
- Go
- Docker/Container

I recommand using container(https://github.com/apple/container) as it is native and removes the need for colima on macs, it also makes working with network easier

# TODO
- Add keepalive so janus keeps session active
- Better error handling
- Add DELETE method to disconnect
- Auth header is needed using bearer token
- Make debug log statements toggle

# HowTo
Just is used to run the different commands to build and run programs, to learn the different commands run
```bash
just
```
This will give a list of commands and what they do
> [!WARNING]
> I have only used docker on mac using colima, not on linux yet(TBD)
>
> A more complete way to use docker is coming
